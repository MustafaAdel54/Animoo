class AnimationConstants {
  static const String appLogoHeroTag = 'app_logo_hero_tag';
  static const Duration splashDelay = Duration(milliseconds: 2000);
  static const Duration routeTransitionDuration = Duration(milliseconds: 700);
}
